services:
  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - apache
  apache:
    image: httpd:2.4
    ports: 
      - "80:80"
    volumes:
      - ./htdocs:/usr/local/apache2/htdocs
      - ./httpd.conf:/usr/local/apache2/conf/httpd.conf